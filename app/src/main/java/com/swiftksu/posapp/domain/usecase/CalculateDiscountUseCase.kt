package com.swiftksu.posapp.domain.usecase

class CalculateDiscountUseCase {
    operator fun invoke(storeDiscount: Float, subTotal: Float): Float {
        // Logic to calculate discount based on store discount and subTotal
        return 0f
    }
}